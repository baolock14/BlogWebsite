@model List<Category>
@if (Model != null)
{
    <ul class="submenu">
        @foreach (var item in Model)
        {
            <li><a class="dropdown-item" href="/@item.Alias">@item.CatName</a></li>
        }
    </ul>
}
